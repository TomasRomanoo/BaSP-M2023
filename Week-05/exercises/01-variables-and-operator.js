//      A - Crear dos variables numéricas y utilizar el operador suma para guardar el valor de la suma de ambos números en una 
//  3er variable;

    var num1 = 20;
    var num2 = 30;
    var suma = num1 + num2;

    console.log("El resultado del ej 01-A = " + suma);
    
//      B - Crear dos variables de tipo String y concatenarlas guardando el resultado en una 3er variable;

    var word1 = 'Pepa';
    var word2 = 'Pig';
    var sentence = word1 + word2;

    console.log("El resultado del ej 01-B = " + sentence);

//      C - Crear dos variables de tipo String y sumar el largo de cada variable (cantidad de letras del string) guardando 
//  el resultado de la suma en una 3er variable (utilizar length);

    var name1 = 'Tomas';
    var name2 = 'Pedro';
    var totalLength = name1.length + name2.length;

    console.log("El resultado del ej 01-C = " + totalLength);

    // Este console.log es para darle mas legibilidad a la consola
    console.log(" "); 